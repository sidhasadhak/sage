import SwiftUI

// ModelCard has been retired — Sage now uses two fixed models.
// This file is kept as a placeholder to avoid stale Xcode references.

struct ModelCard: View {
    var body: some View {
        EmptyView()
    }
}
