import SwiftUI

// ModelLibraryView has been retired — Sage now uses two fixed models
// (Llama 3.2 3B for chat and SmolVLM 256M for photo analysis).
// Model information is available in Settings → AI Models.
// This file is kept as a placeholder to avoid stale Xcode references.

struct ModelLibraryView: View {
    var body: some View {
        EmptyView()
    }
}
